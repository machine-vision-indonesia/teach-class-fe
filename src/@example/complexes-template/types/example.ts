/**
 * @types can be interface or type
 * @naming make it standart interface with I prefix
 */
export interface IExample {
  key1: string
  key2: number
  key3: boolean
}

/**
 * @types can be interface or type
 * @naming make it standart interface with T prefix
 */
export type TExample = {
  key1: string
  key2: number
  key3: boolean
}
