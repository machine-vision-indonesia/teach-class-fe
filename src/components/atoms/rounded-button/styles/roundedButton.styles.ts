export const roundedButonStyle = (size: number) => ({
    borderRadius: '50%',
    width: size,  // adjust the width and height as needed
    height: size, // adjust the width and height as needed
    minWidth: size,
    minHeight: size,
    padding: 0,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
})